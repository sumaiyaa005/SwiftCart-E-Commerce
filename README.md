#### 1) What is the difference between `null` and `undefined`?

কোনো ভ্যারিয়েবল যখন কোনো মান দেওয়া হয়নি তখন সেটি undefined হয় , আর null ব্যবহার করা হয় ইচ্ছাকৃতভাবে ফাঁকা মান দেওয়ার জন্য

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?

map() প্রতিটি উপাদানের উপর কাজ করে এবং একটি নতুন array তৈরি করে।
অন্যদিকে forEach() উপাদানের উপর কাজ করে কিন্তু return করে না।

#### 3) What is the difference between `==` and `===`?

== শুধু মান তুলনা করে, টাইপ দেখে না।
=== মান এবং টাইপ দুইটাই তুলনা করে।

#### 4) What is the significance of `async`/`await` in fetching API data?

async/await ব্যবহার API ডেটা ফেচ করার সময় কোডকে সহজ এবং পড়তে সুবিধাজনক করে। async দিয়ে ফাংশনকে Promise রিটার্ন করতে বলা হয়, আর await ব্যবহার করলে কোড সেই Promise সম্পূর্ণ হওয়া পর্যন্ত অপেক্ষা করে।

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).

Scope মানে হলো কোনো ভ্যারিয়েবল কোথা থেকে ব্যবহার করা যাবে।

1. Global Scope
   যদি কোনো ভ্যারিয়েবল function এর বাইরে ডিক্লেয়ার করা হয়, তাহলে সেটা পুরো ফাইল জুড়ে ব্যবহার করা যায়।
2. Function Scope
   যদি কোনো ভ্যারিয়েবল function এর ভিতরে ডিক্লেয়ার করা হয়, তাহলে সেটা শুধু সেই function এর ভিতরেই ব্যবহার করা যাবে।
3. Block Scope
   let এবং const ব্যবহার করে যদি { } এর ভিতরে ভ্যারিয়েবল ডিক্লেয়ার করা হয়, তাহলে সেটা শুধু ওই block এর ভিতরেই কাজ করবে।

## 🔗 Submission

- **Live Link :** https://teal-treacle-8a1c57.netlify.app/
- **GitHub Repository:** https://github.com/sumaiyaa005/SwiftCart-E-Commerce
